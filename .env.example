# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

DATABASE_URL="mongodb alter example"
# dakshsuvarna46_db_user
# 8UIifhkbIj25aQmU
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
SMTP_HOST="tls"
SMTP_PORT="465"
SMTP_SERVICE="://gmail.com"
SMTP_MAIL=""
SMTP_PASSWORD=""
JWT_PRIVATE_KEY=""
JWT_PUBLIC_KEY=""
# --- Angel One SmartAPI Configs ---
ANGEL_ONE_API_KEY=""
ANGEL_ONE_CLIENT_ID=""
ANGEL_ONE_PASSWORD=""
ANGEL_ONE_TOTP_KEY=""

# --- TimescaleDB & Kafka ---
TIMESCALE_URL="postgresql://postgres:postgres@localhost:5432/timescaledb"
KAFKA_BROKERS="localhost:9092"
# Fyers Configuration
FYERS_APP_ID=""
FYERS_SECRET_ID=""
FYERS_PIN=""

# --- CORS: Allowed WebSocket / HTTP origins (comma-separated for multiple) ---
# app://-  is the Electron app's secure origin
# Add your deployed web domain here if you build a web version
ALLOWED_ORIGIN="*"


TELEGRAM_BOT_TOKEN=""
TELEGRAM_CHAT_ID=""
--------------end ---------------
example=""